variables:
  GO_VERSION: '1.25'
  GOLANGCI_VERSION: 'v2.11'

stages:
  - test

.test:
  stage: test
  rules:
    - if: $CI_PIPELINE_SOURCE == "schedule"
      when: never
    - if: $CI_PIPELINE_SOURCE == "merge_request_event"
    - if: $CI_COMMIT_BRANCH == $CI_DEFAULT_BRANCH
    - if: $CI_COMMIT_TAG

lint:
  extends:
    - .test
  image: docker-registry.selectel.ru/golangci/golangci-lint:$GOLANGCI_VERSION
  tags:
    - kube-dev
  script:
    - golangci-lint run ./...

unit-tests:
  extends:
    - .test
  image: docker-registry.selectel.ru/library/golang:$GO_VERSION
  tags:
    - kube-dev
  script:
    - go vet ./...
    - go test -coverprofile=coverage_unit.txt -covermode count ./...
  artifacts:
    name: cover
    paths:
      - coverage_unit.txt

test-coverage:
  extends:
    - .test
  needs:
    - unit-tests
  dependencies:
    - unit-tests
  image: docker-registry.selectel.ru/library/golang:$GO_VERSION
  tags:
    - kube-dev
  script:
    - go install github.com/boumenot/gocover-cobertura@v1.4.0
    - gocover-cobertura -ignore-gen-files < coverage_unit.txt > coverage.xml
    - go tool cover -func coverage_unit.txt
  coverage: /total:\s+\(statements\)\s+\d+.\d+%/
  artifacts:
    reports:
      coverage_report:
        coverage_format: cobertura
        path: coverage.xml
  allow_failure: true
